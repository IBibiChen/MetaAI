/**
 * 默认安全基础工具包
 *
 * <p>
 * 本包不是 common 工具箱
 * 它只承载无副作用、无权限边界、跨业务复用、可以安全暴露给模型的基础工具
 * 这类工具可以挂载到 ChatClient.defaultTools，也可以进入请求级工具 allowlist
 * 如果一个工具需要租户、用户、权限、审计或外部系统调用，它就不属于 foundation
 *
 * <p>
 * DateTimeTools 是当前基础工具示例
 * 它只读取当前请求时区并返回当前时间，不访问业务数据、不写状态、不依赖租户或用户上下文
 * 后续适合放入本包的工具包括系统只读状态、简单格式化、无业务依赖转换工具
 *
 * <p>
 * 本包不放 RAG 检索工具、数据库查询工具、业务查询工具、业务写入工具或依赖权限上下文的工具
 * 这些工具应放在 agent 或具体业务模块，并在业务模块中绑定权限、租户隔离、审计、幂等和异常处理
 */
package com.metax.tool.foundation;
